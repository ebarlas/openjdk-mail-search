[project]
name = "openjdk-mail-mcp"
version = "0.1.0"
description = "MCP server for OpenJDK mailing list search"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.2.0",
    "httpx>=0.27.0",
]

[project.scripts]
openjdk-mcp = "mcp_server:main"

[tool.uv]
package = true
